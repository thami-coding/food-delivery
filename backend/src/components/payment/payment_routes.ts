// import { Express } from "express";
// import { generatePaymentSignature, updatePaymentStatus } from "./payment_controller";
// import asyncHandler from "express-async-handler"
// import { authenticate } from "../../utils/auth_util";

// // authenticate
// export class PaymentRoutes {
//   constructor(app: Express) {

//     app.route("/api/payment/signature").post(authenticate, asyncHandler(generatePaymentSignature))
//     app.route("/api/payment/complete").post(asyncHandler(updatePaymentStatus))
//   }
// }
